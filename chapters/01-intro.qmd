---
title: "Moleküler Dinamik Simülasyonlarına Giriş"
---

Bu kitapta moleküler dinamik (MD) simülasyonlarını **Türkçe** olarak anlatacağız.  

## Basit bir Python örneği

```{python}
import numpy as np

v = np.random.normal(0, 1, size=1000)
print("Ortalama hız:", np.mean(v))
```
